
  # Liqueur Sales Management App (copia)

  This is a code bundle for Liqueur Sales Management App (copia). The original project is available at https://www.figma.com/design/hYTcEyGqPPQPInyGIETdew/Liqueur-Sales-Management-App--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Grandmasliquors
# front_grandmas
